#include <stdio.h>
#include <time.h>
/* Here contains the Headers */

/* betty style doc for the main function in here */
int main(void)
{
	int n;
	$rand(time(0));
	n = rand() - RAND_MAX / 2;

	/* Body of code */
	return (0);
}
