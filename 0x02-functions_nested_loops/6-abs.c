#include "main.h"
/**
 * main - main function
 * @i: absolute value
 * Description: _abs calculates for the
 * absolute value of any integer,as it is represented
 * by i
 * Return: absolute value of an integer
 */
int _abs(i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
